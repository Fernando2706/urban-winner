import {user, users} from "./user.ts";
import { post, posts } from "./post.ts";
import { comment, comments } from "./comment.ts";

const Query = {
    user,
    users,
    post,
    posts,
    comment,
    comments
}

export default Query
